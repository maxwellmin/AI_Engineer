from django.apps import AppConfig


class DocumentPipelineManagerConfig(AppConfig):
    """Configuration for Document Pipeline Manager app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.document_pipeline_manager"
    verbose_name = "Document Pipeline Manager"
